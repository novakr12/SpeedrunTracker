# Database (matches docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_USER=speedrun
DB_PASSWORD=speedrun
DB_NAME=speedrun

# Auth
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=1d

# Server
PORT=3000
